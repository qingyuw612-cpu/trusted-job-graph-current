[project]
name = "role-normalization-project"
version = "0.1.0"
description = "可审计的岗位概念归一化与新岗位候选发现"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
semantic = ["sentence-transformers>=2.2"]

[tool.unittest]
start-directory = "tests"
